{
  "framework": "vite",
  "installCommand": "npm ci --prefix app && npm ci --prefix app/server",
  "buildCommand": "npm run check:production --prefix app && npm run build --prefix app",
  "outputDirectory": "app/dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
